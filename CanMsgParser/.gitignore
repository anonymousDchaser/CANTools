__pycache__/
*.pyc
*.pyo
.pytest_cache/

# PyInstaller 打包产物（dist/ 现按需求正常提交；build/ 与 *.spec 为可重建中间产物，忽略）
build/
*.spec

# 构建临时垃圾目录（build.py 把上一版 dist/build 改名移到此处，不入库）
.build_trash/

# 构建日志与临时备份
*.log
_diag.log
*.bak_*
build.bak_*
dist.bak_*
build_bak*/
dist_bak*/
oldversion*/
WB101.json

# 调试脚本（临时排查用，不应入库）
tests/_dbg.py
